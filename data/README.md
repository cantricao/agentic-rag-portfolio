# Data Directory

This directory is for storing vector database data and other persistent data used by ChromaDB.

## Structure
- Vector embeddings
- ChromaDB collections
- Index files

**Note:** The contents of this directory are excluded from version control (see `.gitignore`).
Only structural documentation and schemas should be committed.
